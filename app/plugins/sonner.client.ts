// plugins/sonner.client.ts
// Ensure vue-sonner styles are available globally
import 'vue-sonner/style.css'

export default defineNuxtPlugin(() => {
	// No runtime setup needed; this plugin exists to load styles client-side
})